
<nav class="navbar navbar-expand-lg">
  <a class="navbar-brand" href="/">Navbar</a>
  <button class="navbar-toggler " type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active Leftspace">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item Leftspace">
        <a class="nav-link" href="/contact">Contact us</a>
    </ul>
         <ul class="navbar-nav">
         {{#unless göster}}
      <li  class="nav-item Leftspace">
        <a class="nav-link " href="/user/login">Login</a>
      </li>
      {{/unless}}
                {{#unless göster}}
         <li  class="nav-item Leftspace ">
        <a class="nav-link" href="/user/register">Sing up</a>
      </li>
      {{/unless}}
      {{#if göster}}
         <li class="nav-item Leftspace ">
       <div class="dropdown mr-1">
<img class="profileimg" src="{{account.avatar}}" alt="">
  <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
   Hesap ayarları
  </button>
  <div class="dropdown-menu dropdownmargin" aria-labelledby="dropdownMenuButton">
    <a class="dropdown-item" href="/profilesetting">Hesap ayarları</a>
    <a class="dropdown-item" href="#">Siparis geçmişi</i></a>
    <a class="dropdown-item" href="/user/logout">Log out</a>
  </div>
</div>
      </li>
      {{/if}}
      </ul>
</nav>